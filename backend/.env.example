# Server Configuration
PORT=3000
NODE_ENV=development

# Security (auto-generated if not set)
JWT_SECRET=your-secure-jwt-secret-here
# Dedicated HMAC key for short-lived /api/agent-identity tokens. Required:
# this endpoint fails loud instead of reusing JWT_SECRET when unset.
AGENT_IDENTITY_SECRET=your-dedicated-agent-identity-secret-here
WEBHOOK_SECRET=your-secure-webhook-secret-here
SEAL_KEY=your-64-hex-char-seal-key-here

# Flickr (for chat photo storage)
FLICKR_API_KEY=your-flickr-api-key
FLICKR_API_SECRET=your-flickr-api-secret
FLICKR_OAUTH_TOKEN=your-flickr-oauth-token
FLICKR_OAUTH_TOKEN_SECRET=your-flickr-oauth-token-secret

# Regression test — bot API response rate
TEST_DEVICE_ID=your-test-device-id

# Broadcast test device
BROADCAST_TEST_DEVICE_ID=your-broadcast-test-device-id
BROADCAST_TEST_DEVICE_SECRET=your-broadcast-test-device-secret

# AI Support — Direct Anthropic API (priority: Anthropic > Proxy > fallback)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Chat semantic search (pgvector) — optional; if unset, /api/chat/search falls
# back to ILIKE keyword search. BYO: users can set OPENAI_API_KEY in their
# per-device env vars to override this server default.
# CHAT_EMBEDDING_PROVIDER=openai  # or "voyage"
OPENAI_API_KEY=
VOYAGE_API_KEY=

# AI Support — Claude CLI proxy (legacy, used when ANTHROPIC_API_KEY is not set)
CLAUDE_CLI_PROXY_URL=http://claude-cli-proxy.railway.internal:4000
SUPPORT_API_KEY=your-shared-secret-here

# Claude CLI proxy — per-request caller credentials (H3 multi-tenant).
# When both CALLER_DEVICE_ID and CALLER_BOT_SECRET are set, the backend sends
# them on every POST /chat so the proxy fetches THIS deployment's GitHub PAT
# from its vault (/api/device-vars) instead of using its own boot-time env
# token. Leave unset to keep legacy single-tenant behavior (the proxy will
# fall back to EVAULT_LEGACY_BOOT_CLONE env-token path).
CLAUDE_PROXY_CALLER_DEVICE_ID=
CLAUDE_PROXY_CALLER_BOT_SECRET=
# Optional overrides — proxy defaults are used when these are unset.
# REPO_HOST_PATH form: github.com/<org>/<repo>.git
CLAUDE_PROXY_REPO_HOST_PATH=
# Vault key to read the GitHub PAT from (e.g. GIT_HUB2). When unset, proxy
# tries org-scoped keys (GIT_HUB2_<ORG>, GITHUB_TOKEN_<ORG>) then global.
CLAUDE_PROXY_VAULT_KEY=
# Comma-separated org allowlist; refuses /chat when repo_host_path falls
# outside this list. When unset, proxy uses its REPO_ALLOWED_ORGS default.
CLAUDE_PROXY_ALLOWED_ORGS=

# OAuth Social Login
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret

# GitHub (for AI assistant to create issues)
GITHUB_TOKEN=ghp_your_token_here
GITHUB_REPO=HankHuang0516/EClaw

# X (Twitter) API — OAuth 1.0a for publishing
X_CONSUMER_KEY=your-x-consumer-key
X_CONSUMER_SECRET=your-x-consumer-secret
X_ACCESS_TOKEN=your-x-access-token
X_ACCESS_TOKEN_SECRET=your-x-access-token-secret

# DEV.to (Forem API) — Developer community publishing
DEVTO_API_KEY=your-devto-api-key

# WordPress.com — Blog publishing (OAuth2 Bearer token)
WORDPRESS_ACCESS_TOKEN=your-wordpress-access-token

# Telegraph (Telegra.ph) — Instant publishing (optional, auto-creates account if not set)
TELEGRAPH_ACCESS_TOKEN=

# Qiita — Japan developer platform (Bearer token)
QIITA_ACCESS_TOKEN=your-qiita-access-token

# WeChat Official Account (微信公眾號) — China content platform
WECHAT_APP_ID=your-wechat-app-id
WECHAT_APP_SECRET=your-wechat-app-secret

# Tumblr (API v2, OAuth 1.0a)
TUMBLR_CONSUMER_KEY=your-tumblr-consumer-key
TUMBLR_CONSUMER_SECRET=your-tumblr-consumer-secret
TUMBLR_ACCESS_TOKEN=your-tumblr-access-token
TUMBLR_ACCESS_TOKEN_SECRET=your-tumblr-access-token-secret

# Reddit (OAuth2 script app)
REDDIT_CLIENT_ID=your-reddit-client-id
REDDIT_CLIENT_SECRET=your-reddit-client-secret
REDDIT_USERNAME=your-reddit-username
REDDIT_PASSWORD=your-reddit-password

# LinkedIn (OAuth2 Bearer — token from developer.linkedin.com)
LINKEDIN_ACCESS_TOKEN=your-linkedin-access-token
LINKEDIN_PERSON_URN=urn:li:person:your-person-id

# Mastodon (any instance)
MASTODON_ACCESS_TOKEN=your-mastodon-access-token
MASTODON_INSTANCE_URL=https://mastodon.social

# Pexels (free image search for article publishing)
PEXELS_API_KEY=your-pexels-api-key

# Publisher API auth (optional — if not set, publisher endpoints are public)
PUBLISHER_API_KEY=your-publisher-api-key

# Monitoring
# External monitoring secret used by uptime probes / cron to hit /api/monitoring/rental-health.
MONITORING_KEY=your-monitoring-key
# Admin device allowlist — comma-separated deviceIds that can reach /api/admin/*
# and admin-gated endpoints (e.g. /portal/admin/rental-monitor.html).
# Example: ADMIN_DEVICE_IDS=480def4c-2183-4d8e-afd0-b131ae89adcc
ADMIN_DEVICE_IDS=
