<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Remove AD_ID permission added by Google SDK — we don't use advertising -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required for some devices to keep service alive properly if needed, usually BIND_WALLPAPER handles it -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Media selection uses Android picker contracts, which grant access to selected URIs. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Push notifications (required for Android 13+ / API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".ClawApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClawLiveCompanion">

        <service
            android:name=".service.ClawWallpaperService"
            android:enabled="true"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/wallpaper" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Android App Links: /r/ universal entry (redirect-state-machine-spec §3).
                 Verification requires /.well-known/assetlinks.json to list the
                 Play App Signing certificate fingerprint. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="eclawbot.com"
                    android:pathPrefix="/r/" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MessageActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ClawLiveCompanion.TransparentDialog"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Settings"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".PrivacyPolicyActivity"
            android:exported="false"
            android:label="@string/privacy_policy_title"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".FeedbackActivity"
            android:exported="false"
            android:label="@string/feedback"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".FeedbackHistoryActivity"
            android:exported="false"
            android:label="@string/feedback_history_title"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".CrashLogViewerActivity"
            android:exported="false"
            android:label="@string/crash_logs_title"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".DebugLogViewerActivity"
            android:exported="false"
            android:label="@string/debug_logs_title"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".WallpaperPreviewActivity"
            android:exported="false"
            android:label="Wallpaper Preview"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".WallpaperMoreSettingsActivity"
            android:exported="false"
            android:label="@string/wallpaper_more_settings"
            android:parentActivityName=".WallpaperPreviewActivity" />

        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:label="@string/chat_history"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".MissionControlActivity"
            android:exported="false"
            android:label="@string/mission_control"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ScheduleActivity"
            android:exported="false"
            android:label="@string/schedule_title"
            android:parentActivityName=".MissionControlActivity" />

        <activity
            android:name=".OfficialBorrowActivity"
            android:exported="false"
            android:label="@string/official_borrow_title"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".FileManagerActivity"
            android:exported="false"
            android:label="@string/file_manager_title"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".widget.ChatWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/claw_chat_widget_info" />
        </receiver>

        <service
            android:name=".fcm.ClawFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- card_f9b2cc2d: run TtsService in an isolated ":tts" process. A
             startForegroundService() that misses the OS 5s startForeground()
             deadline triggers an ASYNC system kill (ForegroundServiceDidNotStart-
             InTimeException) that no in-process try/catch can intercept. While the
             service shared the main process, that kill took the live-wallpaper
             engine down with it (pure black, needs app restart). Isolating it gives
             the FGS its own idle main looper (so it makes the deadline) AND scopes
             any residual kill to the :tts process, leaving the wallpaper alive. -->
        <service
            android:name=".service.TtsService"
            android:exported="false"
            android:process=":tts"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Facebook SDK -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

    </application>

</manifest>
